{
  "_comment": "MCP client configuration. Credentials/DSN live in the SERVER's env block — the MCP host passes them to the server subprocess; the LLM/agent never sees them. Delete COMPANY_DB_DSN to fall back to the local SQLite file.",
  "mcpServers": {
    "company-database": {
      "command": "python",
      "args": ["mcp_server/server.py"],
      "env": {
        "COMPANY_DB_DSN": "postgresql://mcp_readonly:readonly_pass@localhost:5432/company",
        "COMPANY_DB_PATH": "db/company.db"
      }
    }
  }
}
