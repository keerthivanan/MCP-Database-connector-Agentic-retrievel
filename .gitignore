# Secrets — never commit
.env

# Python
__pycache__/
*.pyc

# Node
frontend/node_modules/
# note: frontend/dist IS committed on purpose — it lets the web app run with
# only Python (no Node needed on the host). Rebuild with `npm run build`.

# Local data & logs (regenerable)
db/company.db
mcp_server/audit.log.jsonl
# note: demo_output.txt IS committed on purpose — it's the proof transcript deliverable
